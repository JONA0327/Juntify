# ========================================
# CONFIGURACIÓN PARA MIGRACIÓN DE DATOS
# ========================================
# Añadir estas variables al archivo .env para configurar la conexión a la BD antigua

# Base de datos antigua local (origen de los datos)
OLD_LOCAL_DB_HOST=127.0.0.1
OLD_LOCAL_DB_PORT=3306
OLD_LOCAL_DB_DATABASE=juntify_old
OLD_LOCAL_DB_USERNAME=root
OLD_LOCAL_DB_PASSWORD=

# Opciones adicionales para la conexión antigua
OLD_LOCAL_DB_SOCKET=

# ========================================
# INSTRUCCIONES DE USO
# ========================================

# 1. Configura las variables de arriba según tu BD antigua
# 2. Ejecuta el comando de migración:
#    php artisan migrate:old-data

# 3. Opciones disponibles:
#    --table=users          # Migrar solo una tabla específica
#    --dry-run              # Solo mostrar qué se migraría
#    --batch-size=500       # Cambiar tamaño del batch (default: 1000)

# 4. Ejemplos de uso:
#    php artisan migrate:old-data --dry-run
#    php artisan migrate:old-data --table=users
#    php artisan migrate:old-data --batch-size=500

# ========================================
# NOTAS IMPORTANTES
# ========================================
# - La BD antigua debe estar accesible desde esta aplicación
# - Se recomienda hacer un backup antes de migrar
# - Usa --dry-run primero para verificar qué se migrará
# - El mapeo de tablas se puede ajustar en MigrateOldDataCommand.php
